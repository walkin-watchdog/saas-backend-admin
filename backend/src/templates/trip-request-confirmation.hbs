<!DOCTYPE html>
<html>
<head>
  <meta charset="utf‑8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Your Trip Request</title>
  <style>
    body{font-family:Arial,Helvetica,sans-serif;line-height:1.6;color:#333}
    .container{max-width:600px;margin:0 auto;padding:20px}
    .header{background:linear-gradient(135deg,#104c57 0%,#ff914d 100%);color:#fff;text-align:center;padding:30px;border-radius:6px 6px 0 0}
    .content{background:#f9f9f9;padding:30px}
    .card{background:#fff;padding:20px;border-left:5px solid #ff914d;border-radius:8px;margin:20px 0}
    .row{margin-bottom:10px}
    .label{font-weight:600;color:#104c57}
    .footer{text-align:center;color:#666;padding:20px}
    .social-row{text-align:center}
    .social-row .icon{display:inline-block;margin:0 6px;width:38px;height:38px;border-radius:50%;background:#fff;line-height:38px}
    .social-row img{width:22px;height:22px;margin:8px auto;display:block}
    .icon.facebook{background:#ffffff}
    .icon.linkedin{background:#ffffff}
    .icon.twitter{background:#ffffff}
    .icon.instagram{background:#ffffff}
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{companyName}}</h1>
    </div>

    <div class="content">
      <h2>Thank You {{sanitize name}}!</h2>
      <p>
        We’ve received your trip request and one of our travel experts will be in
        touch within <strong>24&nbsp;hours</strong>. You can contact us directly at {{companyPhone}}
      </p>

      <div class="card">
        <h3 style="margin-top:0">Your Trip Details</h3>

        <div class="row"><span class="label">Destination:</span> {{sanitize destination}}</div>
        <div class="row"><span class="label">Dates:</span> {{startDate}} to {{endDate}}</div>
        <div class="row"><span class="label">Travellers:</span> {{adults}} Adults{{#if children}}, {{children}} Children{{/if}}</div>
        {{#if budget}}<div class="row"><span class="label">Budget Range:</span> {{budget}}</div>{{/if}}
        {{#if accommodation}}<div class="row"><span class="label">Accommodation:</span> {{accommodation}}</div>{{/if}}
        {{#if transport}}<div class="row"><span class="label">Transport:</span> {{transport}}</div>{{/if}}
        {{#if interests.length}}
          <div class="row"><span class="label">Interests:</span> {{interests}}</div>
        {{/if}}
        {{#if specialRequests}}
          <div class="row"><span class="label">Special Requests:</span><br>{{specialRequests}}</div>
        {{/if}}
      </div>

      <p style="margin:20px 0 0">
        Need to add or change something? Just reply to this email &mdash; we’re happy to help.
      </p>
    </div>

    <div class="footer">
      <p>Discover the Extraordinary</p>
      <p style="margin:12px 0 6px;font-weight:600">Follow us on</p>
      <div class="social-row">
        {{#if FACEBOOK_URL}}
          <a href="{{FACEBOOK_URL}}" class="icon facebook" aria-label="Facebook">
            <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/facebook.svg" alt="Facebook">
          </a>
        {{/if}}
        {{#if LINKEDIN_URL}}
          <a href="{{LINKEDIN_URL}}" class="icon linkedin" aria-label="LinkedIn">
            <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/linkedin.svg" alt="LinkedIn">
          </a>
        {{/if}}
        {{#if X_URL}}
          <a href="{{X_URL}}" class="icon twitter" aria-label="X / Twitter">
            <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/x.svg" alt="X">
          </a>
        {{/if}}
        {{#if INSTAGRAM_URL}}
          <a href="{{INSTAGRAM_URL}}" class="icon instagram" aria-label="Instagram">
            <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/instagram.svg" alt="Instagram">
          </a>
        {{/if}}
      </div>
    </div>
  </div>
</body>
</html>